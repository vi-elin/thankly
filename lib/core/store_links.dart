/// App Store Connect numeric app id (from the App Information page URL).
const appStoreId = '6791693586';

/// Play Store package name, also used as the app's applicationId.
const androidPackageName = 'com.mobileapp.thanklio';
